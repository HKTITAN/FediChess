# Refactor component

Refactor the component(s) the user specified. Preserve behavior; improve structure (e.g. split subcomponents, extract hooks, align with FediChess design and project conventions). Run lint and fix any issues. Use `@/` and conventions from `.cursor/rules/conventions.mdc`.
