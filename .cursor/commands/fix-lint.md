# Fix lint

Run the project linter (e.g. `npm run lint`). Fix every reported error or warning. If the user added context (e.g. a path), focus on that area first. Report what was fixed or confirm no issues.
