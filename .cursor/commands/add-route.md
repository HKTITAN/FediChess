# Add route

Add a new Next.js route (page) under `app/`. Create the page component, link from the right place (e.g. landing or nav), and follow existing layout and data patterns. Use static export–compatible patterns (no server-only APIs). Use `@/` for imports.
